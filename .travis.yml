---
services:
  - docker

env:
#-----------------------------------------------
#
# CentOS
#
#-----------------------------------------------
  - distribution: centos
    version: "7"
    playbook: applications-logs
  - distribution: centos
    version: "7"
    playbook: no-logs
  - distribution: centos
    version: "6"
    playbook: applications-logs
  - distribution: centos
    version: "6"
    playbook: no-logs

#--------------------------------
#
# Fedora
#
#--------------------------------
  - distribution: fedora
    version: "28"
    playbook: applications-logs
  - distribution: fedora
    version: "28"
    playbook: no-logs
  - distribution: fedora
    version: "27"
    playbook: applications-logs
  - distribution: fedora
    version: "27"
    playbook: no-logs
  - distribution: fedora
    version: "26"
    playbook: applications-logs
  - distribution: fedora
    version: "26"
    playbook: no-logs

#--------------------------------
#
# Ubuntu
#
#--------------------------------
  - distribution: ubuntu
    version: "18.04"
    playbook: applications-logs
  - distribution: ubuntu
    version: "18.04"
    playbook: no-logs
  - distribution: ubuntu
    version: "17.10"
    playbook: applications-logs
  - distribution: ubuntu
    version: "17.10"
    playbook: no-logs
  - distribution: ubuntu
    version: "16.04"
    playbook: applications-logs
  - distribution: ubuntu
    version: "16.04"
    playbook: no-logs
  - distribution: ubuntu
    version: "14.04"
    playbook: applications-logs
  - distribution: ubuntu
    version: "14.04"
    playbook: no-logs
  - distribution: ubuntu
    version: "12.04"
    playbook: applications-logs
  - distribution: ubuntu
    version: "12.04"
    playbook: no-logs

#--------------------------------
#
# Debian
#
#--------------------------------
  - distribution: debian
    version: "9"
    playbook: applications-logs
  - distribution: debian
    version: "9"
    playbook: no-logs
  - distribution: debian
    version: "8"
    playbook: applications-logs
  - distribution: debian
    version: "8"
    playbook: no-logs
  - distribution: debian
    version: "7"
    playbook: applications-logs
  - distribution: debian
    version: "7"
    playbook: no-logs

#--------------------------------
#
# Oraclelinux
#
#--------------------------------
  - distribution: oraclelinux
    version: "7"
    playbook: applications-logs
  - distribution: oraclelinux
    version: "7"
    playbook: no-logs
  - distribution: oraclelinux
    version: "6"
    playbook: applications-logs
  - distribution: oraclelinux
    version: "6"
    playbook: no-logs

#--------------------------------
#
# Amazonlinux
#
#--------------------------------
  - distribution: amazonlinux
    version: "2"
    playbook: applications-logs
  - distribution: amazonlinux
    version: "2"
    playbook: no-logs
  - distribution: amazonlinux
    version: "1"
    playbook: applications-logs
  - distribution: amazonlinux
    version: "1"
    playbook: no-logs

#--------------------------------
#
# Opensuse
#
#--------------------------------
  - distribution: opensuse
    version: "42.3"
    playbook: applications-logs
  - distribution: opensuse
    version: "42.3"
    playbook: no-logs
  - distribution: opensuse
    version: "42.2"
    playbook: applications-logs
  - distribution: opensuse
    version: "42.2"
    playbook: no-logs
  - distribution: opensuse
    version: "13.2"
    playbook: applications-logs
  - distribution: opensuse
    version: "13.2"
    playbook: no-logs

#--------------------------------
#
# Archlinux
#
#--------------------------------
  - distribution: archlinux
    version: "rolling"
    playbook: applications-logs
  - distribution: archlinux
    version: "rolling"
    playbook: no-logs

before_install:
  # Build docker container
  - 'sudo docker pull diodonfrost/${distribution}-${version}-ansible:latest'

script:
  - container_id=$(mktemp)

  # Run container and mount ansible role with it
  - 'sudo docker run --detach --privileged -v /sys/fs/cgroup:/sys/fs/cgroup:ro --volume="${PWD}":/etc/ansible/roles/ansible-role-logrotate:ro diodonfrost/${distribution}-${version}-ansible:latest > "${container_id}"'

  # Check syntax
  - 'sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-logrotate/tests/playbooks/${playbook}.yml --syntax-check'

  # Run ansible playbook
  - 'sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-logrotate/tests/playbooks/${playbook}.yml'

  # Test indempotence
  - >
    sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-logrotate/tests/playbooks/${playbook}.yml
    | grep -q 'changed=0.*failed=0'
    && (echo 'Idempotence test: pass' && exit 0)
    || (echo 'Idempotence test: fail' && exit 1)
  # Test role
  - 'inspec exec tests/inspec -t docker://$(cat ${container_id})'

  # Delete container
  - 'sudo docker rm -f "$(cat ${container_id})"'

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
